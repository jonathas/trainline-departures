import { request } from "./common";

describe("# Services", () => {

    it("should return the service");

    it("should return invalid request when the service identifier is not provided");

    it("should return invalid request when the service identifier is badly formatted");

    it("should return invalid request when the date is not provided");

    it("should return invalid request when the date is badly formatted");

});